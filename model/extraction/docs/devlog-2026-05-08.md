# 개발 일지 — 2026-05-08

**담당**: 윤정  
**브랜치**: `feature/yunjeong-extraction-v5`  
**작업 범위**: predict.py 정규식 제거 / v4 데이터 파이프라인 / 재학습 준비

---

## 작업 요약

| 분류 | 내용 | 파일 |
|---|---|---|
| refactor | predict.py 정규식 필터 3종 제거 — layout_normalizer 시너지 | `file/predict.py` |
| docs | eval-base-vs-small: galsan unseen 테스트셋 B 결과 추가 | `docs/eval-base-vs-small-2026-05-07.md` |
| data | v4 학교 데이터 ZIP → TXT 변환 (1,359개) | `data/txt/v4_school_txt/` |
| data | TXT → JSONL 변환 (34,284개 문장) | `data/train/v4_raw_unlabeled.jsonl` |
| data | is_todo_label.py 규칙 라벨링 | `data/train/v4_labeled.jsonl` |
| data | koelectra-small 모델 재검수 + 정제 → 최종 학습 데이터 | `data/train/v4_clean.jsonl` |
| data | v3.1.3 + v4_clean 병합 (소프트 라벨 통일) | `data/train/v4_merged_train.jsonl` |
| feat | HF Hub 업로드 셀 + INPUT_FILE 업데이트 | `file/train_koelectra_base.ipynb` |
| feat | 재라벨링 스크립트 신규 작성 | `scripts/relabel_false_model.py` |
| feat | 병합 스크립트 신규 작성 | `scripts/merge_train_data.py` |

---

## 1. predict.py 정규식 필터 3종 제거

### 배경

백엔드 layout_normalizer(Claude API)가 PDF/HWP 텍스트를 정제해 `\n` 줄 단위로 윤정 모델에 넘기도록 구조가 바뀜.
기존 정규식 3종이 이 구조와 책임이 중복되거나 모델 판단을 덮어쓰는 문제를 일으켰음.

### 제거 항목

| 항목 | 문제 |
|---|---|
| `_HEADER_ONLY` | layout_normalizer가 title을 별도 필드로 분리해 넘기므로 중복 |
| keyword split (`운영시간\|운영방법\|신청방법\|...`) | Claude가 `\n` 줄 단위로 정제된 텍스트를 넘기므로 재분리 불필요. "제출 바랍니다" 식 잘림 잔재 유발 |
| `_NON_TODO_PATTERNS` 18개 | labeling-guide F4_greeting/F3_sender_info에 해당 패턴이 학습 데이터에 False로 충분히 포함됨. 규칙이 모델이 학습한 미묘한 판단을 덮어씌우는 부작용 |

`_OCR_LINE_NOISE` (URL/시간 범위/화살표 전용 줄)는 layout_normalizer와 무관한 형식 노이즈이므로 유지.

---

## 2. galsan unseen 테스트셋 B 평가

### 결과

| 모델 | Accuracy | F1 (할 일) | Precision | Recall |
|---|---|---|---|---|
| v3.1 Small | 68.73% | **0.4168** | 0.2765 | **0.8455** |
| **Base** | **71.88%** | 0.4157 | **0.2865** | 0.7570 |

> 평가 조건: pipeline 기본 threshold(0.5) 사용 — 운영 threshold와 다름. 상대 비교는 유효.

### 해석

- F1은 두 모델이 사실상 동일 (차이 0.001)
- Base가 Accuracy +3.15%p, Precision +1.0%p 우세
- **결론: 테스트셋 A·B 모두에서 Base가 열등하지 않음 → Base 채택 유지**

→ `docs/eval-base-vs-small-2026-05-07.md` 테스트셋 B 섹션 추가.

---

## 3. v4 데이터 파이프라인

### 원본 데이터

| ZIP 파일 | 내부 파일 수 |
|---|---|
| `data/raw/v4_405.Zip` | — |
| `data/raw/v4_413.Zip` | — |
| `data/raw/v4_694.Zip` | — |
| **합계** | **1,119개** |

### 파이프라인 흐름

```
v4_405/413/694.Zip
    ↓ Docker batch_convert.py (BATCH_MODE=batch)
data/txt/v4_school_txt/   1,359개 TXT  (HWP→ODT→TXT, PDF→TXT, 실패 81개)
    ↓ preprocess_txt_to_jsonl.py
data/train/v4_raw_unlabeled.jsonl   34,284개 문장  (급식 파일 13개 스킵)
    ↓ is_todo_label.py --with_reason
data/train/v4_labeled.jsonl
    True 4,599개 (13.4%) / no_match 24,781개
    ↓ relabel_false_model.py (koelectra-small, threshold=0.55)
data/train/v4_relabeled.jsonl
    ↓ _clean_jsonl.py  (len<10 제거 4,731개 / 중복 제거 4,928개)
    ↓ _refine_jsonl.py (model_relabel conf<0.97 → False, 1,498개 재전환)
data/train/v4_clean.jsonl   24,625개  True 35.3% (8,701개)
```

### 라벨링 전략

- **is_todo_label.py**: labeling-guide v3.1.2 기준 규칙 라벨링 (`auto_label.py`는 이전 버전이므로 사용 안 함)
- **model_relabel**: 규칙으로 False가 된 문장 중 koelectra-small conf ≥ 0.97인 것만 True 전환
- **True 비율 35.3%**: 가이드 기준(13%)보다 의도적으로 높게 설정 — 현재 모델이 놓치는 부모 액션 문장 recall 보정 목적

### 정제 기준

| 기준 | 제거 수 |
|---|---|
| len < 10 (날짜 조각, 레이블 단편 등) | 4,731개 |
| 완전 중복 (True 우선 보존) | 4,928개 |
| model_relabel conf < 0.97 → False 재전환 | 1,498개 |

---

## 4. v4_merged_train.jsonl 생성

v3.1.3_dual_labeled.jsonl(22,523개) + v4_clean.jsonl(24,625개) 병합.

v4 데이터에 소프트 라벨(`is_todo_prob`) 부여:

| 출처 | is_todo_prob |
|---|---|
| model_relabel(conf=X) True | X 그대로 (모델 확신도) |
| 규칙 기반 True | 0.90 |
| False | 0.05 |

**최종 병합 결과:**

| | 수 |
|---|---|
| v3.1.3 | 22,523개 |
| v4 | 24,625개 |
| **합계** | **47,148개** |
| True 비율 | 26.2% |
| 필드 | `text / is_todo / is_todo_prob / is_title / label_group` (전체 동일) |

---

## 5. train_koelectra_base.ipynb 업데이트

| 셀 | 변경 내용 |
|---|---|
| cell-6 | `INPUT_FILE = 'v4_merged_train.jsonl'` |
| cell-28 (신규) | "14. HF Hub 업로드" 마크다운 |
| cell-29 (신규) | `huggingface_hub.HfApi.upload_folder()` — `yunjeong116/koelectra-extractor` 자동 푸시 |

### Colab 실행 순서

1. `v4_merged_train.jsonl` 업로드
2. Colab Secrets에 `HF_TOKEN` 설정
3. 셀 순서대로 실행 (A100 권장, T4 가능)
4. 학습 완료 후 cell-29로 HF Hub 자동 푸시
5. NCP 재배포 시 hf_cache 비우고 새 모델 수신 확인

**예상 학습 시간**: T4 ~60분 / A100 ~20분 (47k 문장, 10 epochs, early stopping)

---

## 6. 의사결정 기록

### Small vs Base 모델 선택

| 관점 | Small | Base |
|---|---|---|
| 파라미터 | 14M | 110M |
| seen F1 | 0.8223 | **0.8387** |
| unseen F1 | **0.4168** | 0.4157 (≈동일) |
| 추론 속도 | 빠름 | 느림 |
| 학습 시간 | ~10분 | ~60분 |

→ unseen 성능이 동일하고 seen 데이터에서 Base가 앞서므로 **Base 유지**.  
속도가 중요해지는 시점에 Small 재검토.

### 가정통신문 도메인 특화 포지셔닝

범용 생성형 AI보다 도메인 특화 모델이 나을 수 있는 영역.
v4 데이터 축적 → 재학습 사이클이 이 격차를 벌리는 플라이휠.

---

## 7. 신규 스크립트

| 파일 | 역할 |
|---|---|
| `scripts/relabel_false_model.py` | v4_labeled의 False 문장을 koelectra-small로 재검수, conf 이상 시 True 전환 |
| `scripts/merge_train_data.py` | v3.1.3 + v4_clean 병합, v4에 소프트 라벨 부여 |

---

## 8. 다음 작업

- [ ] Colab에서 koelectra-base 재학습 실행 (`v4_merged_train.jsonl`)
- [ ] 최적 threshold 확인 후 `predict.py BINARY_THRESHOLD` 업데이트
- [ ] HF Hub `yunjeong116/koelectra-extractor` 새 가중치 푸시
- [ ] NCP hf_cache 비우기 + 재배포
- [ ] 4종 통신문으로 정규식 제거 효과 검증 (잘림 잔재 해소 여부)
- [ ] galsan unseen 테스트셋 B 재평가 (새 모델 기준)
- [ ] `labeling-guide.md` 성능 이력 테이블 업데이트
