# 2026-05-01 ~ 05-03 워크로그 — OCR 실험 완료 + 학습 데이터 라벨링 파이프라인

## 한 줄 요약

ML Kit Korean OCR 5라운드 실험으로 엔진 채택 확정, OcrActivity 전체 구현, 학습 데이터 dedup → Gemini 라벨링 시도 (3-flash-preview 98% 성공률, 월 한도 7.2%에서 초과).

---

## 머지된 PR

| PR | 제목 | 핵심 |
|---|---|---|
| #80 | feat(sejong): OCR + 파서 + 번역기 통합 | OcrActivity, parser 이미지 지원, translator OCR 노이즈 정제 |
| #84 | feat(data): 라벨링 스크립트 incremental 저장 및 진행률 출력 | 500행 단위 저장, data/processed .gitignore |

---

## OCR 실험 결과 (세종, 2026-05-01)

### 엔진 비교

| 모델 | 평균 CER | F1 | 결과 |
|------|---------|-----|------|
| **ML Kit Korean** | **35.3%** | **0.95~0.97** | **채택** |
| EasyOCR | 98.2% | — | 한국어 인식 실패 |
| Tesseract | 96.8% | — | 한국어 인식 실패 |
| PaddleOCR | — | — | 환경 호환성 오류 |

→ ML Kit Korean 단독 채택. Tesseract는 서버사이드 fallback 용도로만 유지.

### 라운드별 최고 기록 (overall score)

| 라운드 | 주요 변경 | 정면 | 기울임 | 구겨짐 |
|--------|----------|------|-------|-------|
| R1 | 기본 빌드, 낮 촬영 | 0.8122 | 0.8077 | 0.8108 |
| R2 | 밤 촬영, 3종 variant | 0.8183 | 0.8155 | 0.8242 |
| R3 | EXIF 회전 보정 추가 | 0.8147 | 0.8177 | 0.8222 |
| R4 | warped variant 추가 | 0.8196 | 0.8210 | 0.8217 |
| R5 | 6종 전 조합 + BEST 자동 선택 | 0.8195 | 0.8180 | 0.8219 |

→ 실험 최고치 0.8242. 목표 CER ≤ 0.10은 미달이나, Quality Gate 0.80 기준은 통과.

---

## 핵심 구현 (세종)

### OcrActivity.java (신규)

- 카메라 촬영 → FileProvider URI → EXIF 회전 보정
- 전처리 2종 variant: raw + grayscale+CLAHE
- ML Kit Korean TextRecognition 각 variant 실행 → best 선택
- Quality Gate Java 포팅: `text_quality × 0.62 + pattern × 0.38 ≥ 0.80`
- 미통과 시: "원문 확인 필요" 경고 + 재촬영 / 강제 전송 선택 UI
- 업로드: `/notice/upload-self` (parent_id + file, X-User-Id = parentId)

### Quality Gate 임계값 변경

- 0.90 → **0.80** (실험 최고치 0.8242 기반 조정)
- 이유: 0.90은 현재 ML Kit 성능 범위 밖. 0.80이 실용적 기준.

### OCR 버튼 위치 변경

- 선생님 홈 → **학부모 홈** ("📷 종이 통신문 사진 번역")
- 이유: 종이 통신문 사진 촬영은 학부모 역할

### Android readTimeout

- 90s → **180s** (OCR 처리 + 번역 + 업로드 통합 흐름 커버)

### parser.py 변경 (태수님 피드백 반영)

- `_odt_to_text(mark_header=False)`: HWP 표 헤더 마킹 opt-in. True 시 첫 행에 `[헤더] cell1 | cell2` 접두사
- `_image_to_text()`: Tesseract 서버사이드 OCR 추가 (서버 fallback용)
- `IMG_EXTS = {".jpg", ".jpeg", ".png", ".webp"}`: parse_bytes_to_text에서 이미지 분기

### translator.py 변경

- `_OCR_NOISE`: 특수 기호(`□■▣▷◆` 등) 정규식 정제
- `_clean_for_translation()`: 번역 전 OCR 아티팩트 제거
- `translate_short_sentence()` 적용

### /notice/upload-self 엔드포인트 (태수님 구현, 세종 Android 연동)

- parent_id + file만 받음. teacher_id는 내부에서 parent_id로 대체
- 학부모가 종이 통신문을 직접 업로드하는 전용 경로

---

## 학습 데이터 파이프라인 (세종, 2026-05-02~03)

### 데이터 dedup

| 항목 | 수 |
|------|---|
| 원본 v3_school.jsonl | 35,219행 |
| dedup 후 v3_school_dedup.jsonl | 20,843행 |
| 제거된 중복 | 14,376행 |

dedup 기준: `text` 필드 완전 일치

### prepare_todo_labels.py 업데이트

- **incremental JSONL 저장**: 500행마다 flush — 중단돼도 그 시점까지 보존
- **진행률 출력**: `[PROGRESS] 500/20843 (2.4%) | draft rows: 1722 | gemini ok/fail: 295/5`
- 미사용 함수 정리: `make_draft_rows`, `write_jsonl` 삭제 (main() 인라인으로 대체)

### Gemini 라벨링 시도 결과

| 시도 | 모델 | 결과 |
|------|------|------|
| 1차 | gemini-1.5-flash | 404 (모델 미지원) |
| 2차 | gemini-2.5-flash | 100% timeout (30s, 60s 동일) |
| 3차 | gemini-3-flash-preview | **98.6% 성공** → 1,500행(7.2%)에서 월 한도(429) 초과 |

- 3차 시도 결과: 5,383개 draft row JSONL 저장됨, gemini_segment 처리 1,021행
- 나머지 19,343행은 rule_split 상태 (API 한도 해소 후 재개 필요)

---

## 실험 — rule_split vs gemini_segment 품질 비교

**목적**: Gemini 세그멘테이션이 학습 데이터 품질에 얼마나 기여하는지 정량화  
**방법**: Gemini 처리된 원문 1,040개에 rule_split 재실행 후 동일 기준으로 비교  
**실행**: `prepare_todo_labels.py` 내부 함수 재활용, 추가 API 호출 없음

| 항목 | Gemini | Rule |
|------|--------|------|
| 원문당 평균 세그멘트 수 | **4.62개** | 3.71개 |
| Gemini가 더 세세하게 분리한 비율 | 58.8% | — |
| 평균 todo 라벨 수 | **1.36개** | 0.44개 |
| review_required 평균 | **0.01개** | 3.29개 |
| 세그멘트 평균 길이 | 39.5자 | 87.1자 |

**해석**
- Gemini는 의미 단위로 세세하게 분리(4.6개 vs 3.7개), 할 일 판단 3배 정확, 검수 요청 거의 없음
- Rule은 큰 덩어리 유지, todo 보수적 라벨링, review_required 3.29개 → 검수 부담 크고 학습 노이즈 우려
- **결론: 학습 데이터 품질 기준 Gemini 세그멘테이션 명확히 우위 → Gemini 한도 해소 후 나머지 93% 전량 Gemini 처리 예정**

> **윤정님 참고**: 현재 rule_split 상태 draft row는 review_required가 과다 표시됨. 모델 학습 전 Gemini 재처리 완료 후 데이터 사용 권장.

---

## 설계 결정

| 결정 | 이유 |
|------|------|
| ML Kit Korean 단독 채택 | EasyOCR/Tesseract 한국어 CER 97%+ — 실용 불가 |
| Quality Gate 0.80 | 실험 최고치 0.82 기반. 0.90은 현재 달성 불가 범위 |
| OCR 버튼을 학부모 홈에만 | 선생님은 디지털 파일 보유. 학부모가 종이 수령 후 촬영하는 시나리오 |
| /notice/upload-self 분리 | 학부모 자가 업로드는 선생님 권한 불필요. teacher_id 노출 방지 |
| mark_header opt-in | 태수님 요청 — 기본 동작 변경 없이 표 헤더 마킹 선택 가능하게 |
| Gemini 3-flash-preview | 2.5-flash 6월 종료 예정, 3버전으로 선제 전환 |

---

## 다음 우선 과제

1. **Gemini 라벨링 재개** — AI Studio 월 한도 증액 후 나머지 19,343행 처리
2. **표 2-pass OCR** — 표 영역 crop → 재인식으로 구겨짐 CER 개선 (구겨짐 현재 37.3%)
3. **predict.py 정합성** — 태수님 이슈: whitelist vs blacklist 불일치 확인 필요
4. **라벨링 완료 후 모델 재학습** — v3_labeled.jsonl → 윤정님 KoELECTRA fine-tune

---

## 메모

- `model/extraction/data/v3_school_dedup.jsonl`: 현재 git 미추적 (untracked). 라벨링 완료 후 커밋 예정
- `data/processed/`: .gitignore 추가됨 — 대용량 draft 데이터는 로컬 보관
- Gemini 재개 명령어:
  ```
  python scripts/prepare_todo_labels.py \
    --input_path model/extraction/data/v3_school_dedup.jsonl \
    --output_jsonl_path data/processed/todo_labeled_draft.jsonl \
    --output_csv_path data/processed/todo_labeled_draft.csv \
    --use_gemini_segment \
    --gemini_model gemini-3-flash-preview \
    --gemini_timeout 60
  ```
