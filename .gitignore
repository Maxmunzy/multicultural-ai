# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
*.egg

# 환경변수
.env
.env.*
!.env.example
*.env.local

# ML 체크포인트 / 대용량 파일
ml/*/checkpoints/
ml/*/best_model/
*.pt
*.bin
*.onnx
*.safetensors

# 경이 분류 모델 체크포인트 (허깅페이스 허브에 별도 관리)
model/classification/checkpoints/

# 컨테이너 작업 영역 (학습 데이터·OCR·임시 변환물)
backend/data/

# 평가 스크립트 실행 결과물 (재생성 가능)
model/translation_tts/outputs/

# 번역 평가 점수 파일 (모델별 JSONL, 재생성 가능 + 대용량)
model/translation_tts/scores/

# 번역 평가 중간 산출물 (재생성 가능)
model/translation_tts/eval_inputs*.jsonl
model/translation_tts/eval_inputs_gemini*.json
model/translation_tts/p11_eval_input_all.json
model/translation_tts/translation_quality_eval_v1*.jsonl
model/translation_tts/eval_chunks/reeval_p11e_all.jsonl
model/translation_tts/eval_chunks/pipeline_stages_vi_en.jsonl
model/translation_tts/evaluator_prompt.md
model/translation_tts/p11_code_review_prompt.md
model/translation_tts/p11_translation_eval_prompt.md
model/translation_tts/results_summary_template.md
model/translation_tts/run_collect_manual_eval_scores.py
model/translation_tts/run_translation_quality.py
model/translation_tts/run_update_p11_scores.py

# Android build outputs and local settings
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/local.properties
*.apk
*.aab
*.keystore


# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# TTS 출력물
tts/*.mp3
tts/*.wav
# 런타임 업로드 (가통문 원본 PDF/이미지) + TTS mp3 — 폴더는 git에 유지
backend/static/notices/*
!backend/static/notices/.gitkeep
backend/static/tts/*
!backend/static/tts/.gitkeep

#클로드
.claude

# nested repo 방지
multicultural-ai/
multicultural-school-ai/

# 미팅 자료 (디코로만 공유, 깃에는 안 올림)
docs/meetings/

# 학습 데이터 draft (대용량, 미완성)
data/processed/
data/raw/

# 용어사전 후보 아카이브 (term_glossary.csv에 전부 반영 완료)
model/translation_tts/archive/

# Firebase 인증 파일 (절대 git에 올리면 안 됨 — secret)
android/app/google-services.json
backend/*firebase-adminsdk*.json
backend/firebase-admin-key.json

# 발표 자료 작업 임시 폴더 (xlsx 풀어서 분석한 결과)
tmp_extract/

# sentence_extraction 학습 데이터/원본 PDF/임시 산출물 (대용량, 재생성 가능)
sentence_extraction/data/all_pdfs/
sentence_extraction/data/converted_pdfs/
sentence_extraction/data/heldout_20/
sentence_extraction/data/hwp/
sentence_extraction/data/hwp_to_pdf/
sentence_extraction/data/page_images/
sentence_extraction/data/extra_*/
sentence_extraction/data/seongnam_pdfs/
sentence_extraction/data/taepyeong_pdfs/
sentence_extraction/data/90장/
sentence_extraction/data/v5_pilot/
sentence_extraction/data/test_unseen_*.pdf
sentence_extraction/data/*.jsonl
sentence_extraction/data/*.log
# Colab 업로드 묶음 (PDF zip/tar + 사본 jsonl) — 학습 끝나면 폐기
sentence_extraction/colab_upload_*/pdfs.zip
sentence_extraction/colab_upload_*/pdfs.tar
sentence_extraction/colab_upload_*/*.jsonl
